'use strict';

function(){

    angular
        .controller('ticketApp', timeData);

    function timeData(CONST){
        if (click === foodButtonData){
            moment().fromNow();
        }
    }
}